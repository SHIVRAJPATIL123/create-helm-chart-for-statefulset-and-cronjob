apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "my-demo.fullname" . }}
  namespace: {{ include "Release.Namespace" . }}
  labels: 
    {{- include "my-demo.labels" $ | nindent 4 }}
    
